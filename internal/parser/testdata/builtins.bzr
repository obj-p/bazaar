package builtins

template BuiltinFunctions(list [string], items [int]) {
    // Basic built-in functions
    for i in range(0, 10) {}
    for (idx, val) in enumerate(list) {}
    var size = len(items)

    // Complex expressions with built-ins
    if len(items) > 0 {}
    for i in range(0, len(list)) {}

    // Built-ins in various contexts
    var rangeVar = range(1, 5)
    var enumerateVar = enumerate(items)
    var lengthCheck = len(list) + len(items)

    // Nested built-in calls
    for (i, item) in enumerate(range(0, len(list))) {}

    // Built-ins with complex arguments
    for entry in enumerate(list) {}
    var complexRange = range(len(items), len(items) + 10)
}
