package edgecases

enum EmptyEnum {}

enum TrailingCommaEnum {
    a, b, c,
}

component EmptyComponent {}

data EmptyData {}

template EmptyTemplate {}

func EdgeCases() {
    var component = "foo"
    var data = "bar"
    var modifier = "baz"
    var package = "qux"
    var len = "foo"
    var enumerate = "bar"
    var range = "baz"

    if something {}
    if something({}) {}
    // TODO: This is the ambiguous case - temporarily commented out
    // if (something {})
    var arrayWithTrailing = [1, 2, 3,]
    var mapWithTrailing = {a: 1, b: 2,}
    for (a, b,) in something {}
    var callable = SomeCallable() {
        return 42
    }
    var callable = SomeCallable {
        return 42
    }
}
