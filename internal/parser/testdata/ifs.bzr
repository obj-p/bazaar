package ifs

template Ifs(foo bool, bar bool) {
  if foo != bar {}

  if foo == bar {}

  if foo == bar && foo != true {}

  // TODO(jason.prasad): Fix nested expressions
  // if (foo == bar || (foo != true)) {}
}
