package ifs

template Ifs(foo bool, bar bool, baz bool?) {
	if foo != bar {}

	if foo == bar {}

	if foo == bar && foo != true {}

	// TODO(jason.prasad): Fix nested expressions
	// if (foo == bar || (foo != true)) {}

        // TODO(jason.prasad): More complex expressions
        // if var baz, foo == bar == baz {}
        // if var baz = baz, foo == bar == baz {}
}
