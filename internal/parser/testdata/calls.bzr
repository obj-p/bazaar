package calls

func Calls() {
    Print("Hello, world!")
    BlockLiteral({})
    NamedArgs(first = 1337, second = "second")
    TrailingBlockLiteral {}

    // Complex trailing commas from edge-cases.bzr
    call(
        first,
        second,
    )
}
